[package]
# be a sign or warning of (an imminent event, typically an unwelcome one).
name = "presage"
version = "0.3.0"
authors = ["Gabriel Féron <g@leirbag.net>"]
edition = "2021"

[dependencies]
libsignal-service = { git = "https://github.com/whisperfish/libsignal-service-rs", rev = "72503de963331e3afd28ab837965af7ca7203b80" }
libsignal-service-hyper = { git = "https://github.com/whisperfish/libsignal-service-rs.git", rev = "72503de963331e3afd28ab837965af7ca7203b80" }

async-trait = "0.1"
base64 = "0.12"
futures = "0.3"
hex = "0.4.2"
log = "0.4.8"
rand = "0.7"
serde = "1.0"
serde_json = "1.0"
thiserror = "1.0"
sled = { version = "0.34", optional = true }
url = "2.2.2"
libsodium-sys = { version = "0.2.7", optional = true }
secrets = { version = "1.2.0", features = [
    "use-libsodium-sys",
], optional = true }

bytes = "1"
prost = "0.10"

[build-dependencies]
prost-build = "0.10"

[dev-dependencies]
# for tests
quickcheck = "1.0.3"
quickcheck_async = "0.1"

# for examples
anyhow = "1.0"
chrono = { version = "0.4", default-features = false, features = ["serde", "clock"] }
clap = { version = "3.0", features = ["derive"] }
directories = "3.0"
env_logger = "0.7"
futures = "0.3"
hex = "0.4"
log = "0.4"
mime_guess = "2.0"
rand = "0.7"
serde_json = "1.0"
tempfile = "3.3"
tokio = { version = "1.0", features = [
    "macros",
    "rt-multi-thread",
    "io-std",
    "io-util",
] }
qr2term = { version = "0.2.2" }

[features]
default = ["sled-store"]
quirks = []
sled-store = ["sled"]
secret-volatile-store = ["libsodium-sys", "secrets"]
volatile-store = []

#[patch."https://github.com/whisperfish/libsignal-service-rs.git"]
#libsignal-service = { path = "../libsignal-service-rs/libsignal-service" }
#libsignal-service-hyper = { path = "../libsignal-service-rs/libsignal-service-hyper" }

[patch.crates-io]
"curve25519-dalek" = { git = 'https://github.com/signalapp/curve25519-dalek', branch = 'lizard2' }
