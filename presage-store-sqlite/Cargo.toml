[package]
name = "presage-store-sqlite"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1.83"
bytes = "1.8.0"
chrono = "0.4.38"
hex = "0.4.3"
presage = { path = "../presage" }
presage-store-cipher = { path = "../presage-store-cipher", optional = true }
prost = "0.13.3"
serde_json = "1.0.132"

sqlx = { version = "0.8.2", features = ["sqlite", "uuid"] }
thiserror = "1.0.65"
tracing = "0.1.40"
uuid = { version = "1.11.0", features = ["v4"] }
